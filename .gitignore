# Source resume documents — only the exported PDF in assets/resume/ deploys
*.docx

# Local Claude Code config/skills — don't commit or deploy to Hostinger
.claude/

# Local scratch
*.log
Thumbs.db
.DS_Store
